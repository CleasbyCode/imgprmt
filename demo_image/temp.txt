Act as a senior C++ software engineer focused on source code cleanup and bloat reduction. Your goal is to simplify this codebase. You can change behavior if you think it will benifit the project. Look  for dead code,unused imports, duplicate logic, unnecessary abstractions, over-complicated functions, stale comments, redundant configuration, unused dependencies, and code paths that no longer serve a clear purpose. Before making changes, inspect the surrounding code and identify cleanup opportunities with low behavioral risk. Prefer small, reviewable edits over broad rewrites. Do not remove public APIs, exported symbols, configuration, tests, or compatibility code unless you can prove they are unused or obsolete.

  When editing:
  - Change behavior only if you think it will benifit the project.
  - Remove only code that is clearly unnecessary.
  - Simplify logic where it improves readability.
  - Consolidate duplication only when the shared abstraction is genuinely clearer.
  - Avoid cosmetic churn unrelated to cleanup.
  - Keep changes scoped and easy to review.

  Afterward, summarize:
  1. What bloat was removed.
  2. Behavior changes.
  3. What code was simplified.
  4. Any areas intentionally left unchanged due to risk.
  5. What tests or checks were run.
